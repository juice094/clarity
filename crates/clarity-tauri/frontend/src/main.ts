import { createApp } from 'vue'
import App from './App.vue'

// Kimi design tokens — injected as CSS custom properties
import './styles/kimi-tokens.css'
import './styles/kimi-conversation.css'

createApp(App).mount('#app')
