import { IconDisplay } from "@lucide";
import { Theme } from "../theme.slint";

// 统一 Lucide 图标封装，默认使用主题次要文字色与常规尺寸。
export component LucideIcon inherits IconDisplay {
    stroke: Theme.text-secondary;
    size: 18px;
    stroke-width: 1.5;
}
