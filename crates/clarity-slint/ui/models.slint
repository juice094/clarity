// 共享数据模型

/// 树形导航项（由 Rust 后端拍平后注入）。
export struct TreeItem {
    id: string,
    label: string,
    depth: int,
    is-expanded: bool,
    has-children: bool,
}

/// 会话摘要。
export struct SessionStatus {
    name: string,
    message-count: int,
    is-active: bool,
}
